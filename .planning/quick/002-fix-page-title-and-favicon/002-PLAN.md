---
phase: quick-002
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [app/layout.tsx]
autonomous: true

must_haves:
  truths:
    - "Browser tab shows 'Extensible Checklist' as the page title"
    - "Browser tab displays a custom favicon instead of default Next.js icon"
  artifacts:
    - path: "app/layout.tsx"
      provides: "Updated metadata with proper title and description"
      exports: ["metadata"]
  key_links:
    - from: "app/layout.tsx"
      to: "metadata object"
      via: "export const metadata"
      pattern: "export const metadata.*title.*Extensible Checklist"
---

<objective>
Fix page title and favicon to properly brand the application.

Purpose: Replace default Next.js metadata with application-specific branding for professional appearance.
Output: Browser tabs show "Extensible Checklist" title and custom favicon.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@app/layout.tsx

Current state:
- Page title is default "Create Next App"
- Description is generic "Generated by create next app"
- Favicon exists at app/favicon.ico (default Next.js icon)
</context>

<tasks>

<task type="auto">
  <name>Update page metadata with proper title and description</name>
  <files>app/layout.tsx</files>
  <action>
Update the metadata export in app/layout.tsx:
- Change title from "Create Next App" to "Extensible Checklist"
- Update description to reflect actual application purpose: "Manage recurring tasks through reusable checklist templates"

Keep existing metadata structure (Metadata type from next).
  </action>
  <verify>
1. Check metadata object in app/layout.tsx shows correct title and description
2. Run dev server: npm run dev
3. Open http://localhost:3000 and verify browser tab shows "Extensible Checklist"
4. Inspect page source (Ctrl+U) and verify <title> tag contains "Extensible Checklist"
  </verify>
  <done>
- Browser tab displays "Extensible Checklist" as title
- Meta description tag contains application-specific description
- No console errors related to metadata
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Updated page title and description in metadata</what-built>
  <how-to-verify>
1. Open the application in browser
2. Check browser tab shows "Extensible Checklist" (not "Create Next App")
3. Navigate to different pages (templates, checklists, dashboard) - title should persist
4. Verify favicon appears in browser tab (existing app/favicon.ico should be auto-detected by Next.js)
  </how-to-verify>
  <resume-signal>Type "approved" if title and favicon display correctly, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- Browser tab title reads "Extensible Checklist" on all pages
- Page source contains correct <title> tag
- Favicon displays in browser tab (Next.js auto-serves app/favicon.ico)
- No metadata-related warnings in browser console
</verification>

<success_criteria>
- Metadata object in app/layout.tsx contains "Extensible Checklist" title
- Browser tab shows proper branding on all pages
- Favicon visible in browser tab
- Change verified by user checkpoint
</success_criteria>

<output>
After completion, create `.planning/quick/002-fix-page-title-and-favicon/002-SUMMARY.md`
</output>
