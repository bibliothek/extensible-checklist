---
phase: 10-documentation-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [README.md]
autonomous: true

must_haves:
  truths:
    - "Developer can follow README to set up local SQLite environment from scratch"
    - "README accurately reflects SQLite instead of PostgreSQL throughout"
    - "Local development section provides correct commands for SQLite workflow"
  artifacts:
    - path: "README.md"
      provides: "SQLite-focused local development instructions"
      contains: "SQLite"
      min_lines: 300
  key_links:
    - from: "README.md local setup"
      to: ".env.example"
      via: "Environment variable instructions"
      pattern: "DATABASE_URL.*file:"
    - from: "README.md"
      to: "docker-compose.yml"
      via: "Docker development section"
      pattern: "docker-compose up"
---

<objective>
Update README.md to reflect SQLite database instead of PostgreSQL throughout all sections, providing accurate setup instructions for local development with SQLite.

Purpose: Ensure developers can successfully set up local environment using SQLite (current README still references PostgreSQL from v1.0/v2.0, which is no longer accurate after Phase 8 database migration).

Output: Updated README.md with SQLite-specific instructions, accurate tech stack, and simplified local setup (no external database service required).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

# Phase 8 & 9 summaries (SQLite migration and Docker/Azure updates)
@.planning/phases/08-database-migration/08-01-SUMMARY.md
@.planning/phases/09-docker-azure-update/09-01-SUMMARY.md
@.planning/phases/09-docker-azure-update/09-02-SUMMARY.md

# Current files
@README.md
@.env.example
@docker-compose.yml
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README.md Tech Stack and Features sections for SQLite</name>
  <files>README.md</files>
  <action>
Update the following sections of README.md to reflect SQLite instead of PostgreSQL:

1. **Features section** (line 14): Change "Persistent State: PostgreSQL database ensures data reliability" to "Persistent State: SQLite database with file-based storage"

2. **Tech Stack section** (line 20): Change "Prisma ORM with PostgreSQL database" to "Prisma ORM with SQLite database"

3. Remove any remaining PostgreSQL references throughout the document.

Context: Phase 8 migrated from PostgreSQL to SQLite. README still has PostgreSQL references from v1.0/v2.0 that are now inaccurate.
  </action>
  <verify>
Verify changes:
```bash
# Should NOT find PostgreSQL references in main sections
findstr /I "postgresql postgres" README.md

# Should find SQLite references
findstr /I "sqlite" README.md
```
  </verify>
  <done>
- Features section mentions SQLite (not PostgreSQL)
- Tech Stack section lists SQLite as database
- No misleading PostgreSQL references remain in top sections
  </done>
</task>

<task type="auto">
  <name>Task 2: Update README.md Local Development section for SQLite</name>
  <files>README.md</files>
  <action>
Replace the "Local Development" section (lines 25-93) with SQLite-focused instructions:

**New structure:**
1. Prerequisites: Node.js 20 or higher (remove Docker Desktop requirement - it's optional)
2. Setup steps:
   - Clone repository
   - Install dependencies
   - Configure environment variables (reference .env.example which already has SQLite config)
   - Run database migrations with `npm run db:push` (no docker-compose needed)
   - Start development server
   - Access application

3. Development commands (keep existing, they're correct)

**Key changes:**
- Remove step 4 "Start PostgreSQL database" with `docker-compose up postgres` command (no longer needed with SQLite)
- Update step 3 environment variables description to match .env.example (file:./prisma/dev.db format)
- Clarify that SQLite database file is created automatically on first migration
- Simplify flow: install → configure → migrate → run (no separate database service)

Reference .env.example for accurate DATABASE_URL format (already updated to SQLite in Phase 8).
  </action>
  <verify>
Verify local development section:
```bash
# Check that docker-compose postgres command is removed from local dev
findstr /C:"docker-compose up postgres" README.md

# Check that SQLite file path format is mentioned
findstr /C:"file:" README.md

# Check that migrations command is present
findstr /C:"db:push" README.md
```
  </verify>
  <done>
- Local development section does NOT require docker-compose for database
- Environment variables section references SQLite file path format
- Setup flow is simplified: no external database service needed
- Migrations command (npm run db:push) creates SQLite database automatically
  </done>
</task>

<task type="auto">
  <name>Task 3: Update README.md Production Deployment section for SQLite and Azure Files</name>
  <files>README.md</files>
  <action>
Update "Production Deployment (Azure)" section (lines 143-345) to reflect SQLite + Azure Files architecture:

**Updates needed:**

1. **Prerequisites section** - Keep as-is (correct)

2. **Azure Setup sections:**
   - Keep ACR setup (1 & 2) - unchanged
   - Keep App Service setup (3) - unchanged
   - **Section 4 (Environment Variables)**: Update DATABASE_URL example from PostgreSQL connection string to `file:/app/data/dev.db`
   - **Section 4**: Update table to show `DATABASE_URL=file:/app/data/dev.db` instead of PostgreSQL connection string
   - **Section 4**: Remove "Database Options" subsection mentioning Azure Database for PostgreSQL - this is no longer the deployment model
   - **Section 4**: Add note that SQLite requires Azure Files for persistence (reference docs/DEPLOYMENT.md for complete setup)
   - Keep sections 5-7 (Health Check, Publish Profile, GitHub Secrets) - unchanged

3. **Deployment Flow section** - Keep as-is (accurate)

4. **Troubleshooting section** - Remove "Database connection errors" subsection about PostgreSQL firewall rules and SSL (not applicable to SQLite)

**Important:** Do NOT duplicate the comprehensive Azure Files setup from docs/DEPLOYMENT.md. Instead, add a note in section 4 directing users to docs/DEPLOYMENT.md for complete Azure Files configuration steps. The README should provide quick reference, while docs/DEPLOYMENT.md (created in Phase 9 Plan 02) contains the detailed 877-line guide.

Reference: docs/DEPLOYMENT.md was created in Phase 9 Plan 02 with comprehensive Azure Files setup instructions.
  </action>
  <verify>
Verify production section updates:
```bash
# Should show SQLite file path for DATABASE_URL
findstr /C:"file:/app/data/dev.db" README.md

# Should reference DEPLOYMENT.md for Azure Files setup
findstr /C:"DEPLOYMENT.md" README.md

# Should NOT have PostgreSQL connection string examples in Environment Variables
findstr /C:"postgresql://" README.md
```
  </verify>
  <done>
- DATABASE_URL environment variable shows SQLite file path (file:/app/data/dev.db)
- Production section references docs/DEPLOYMENT.md for Azure Files setup
- PostgreSQL-specific troubleshooting removed (firewall, SSL, connection strings)
- Azure deployment section accurately reflects simplified SQLite architecture
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. **Completeness check:**
   ```bash
   # Verify no PostgreSQL references remain in critical sections
   findstr /I /N "postgresql postgres" README.md

   # Should only appear in git history or comments if at all
   ```

2. **Accuracy check:**
   ```bash
   # Verify SQLite is mentioned in key sections
   findstr /I /N "sqlite" README.md

   # Verify docs/DEPLOYMENT.md is referenced
   findstr /N "DEPLOYMENT.md" README.md
   ```

3. **Coherence check:** Read README.md sections to ensure:
   - Local development flow makes sense (no external database needed)
   - Production deployment references comprehensive guide
   - All commands and paths are accurate

4. **Requirement coverage:**
   - DOC-01: README updated with SQLite setup instructions ✓
   - DOC-03: Local development instructions updated for SQLite ✓
</verification>

<success_criteria>
1. README.md accurately reflects SQLite database throughout all sections
2. Local development section provides correct SQLite setup flow (no docker-compose for database)
3. Production deployment section references docs/DEPLOYMENT.md for Azure Files setup
4. No misleading PostgreSQL references remain in setup instructions
5. Developer can follow README to set up local SQLite environment from scratch
</success_criteria>

<output>
After completion, create `.planning/phases/10-documentation-verification/10-01-SUMMARY.md`
</output>
