# Database Configuration
# Copy this file to .env and update with your actual database credentials

# SQLite database file path
# Format: file:./path/to/database.db
# The database file will be created automatically on first migration
DATABASE_URL="file:./prisma/dev.db"

# Auth.js Configuration
# NEXTAUTH_URL should match your application URL
NEXTAUTH_URL="http://localhost:3000"

# NEXTAUTH_SECRET is used to encrypt tokens and email verification hashes
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET="your-secret-here-generate-with-openssl"

# Access Control Configuration
# Comma-separated list of email addresses approved for signup
# Example: APPROVED_EMAILS="user1@example.com,user2@example.com,admin@company.com"
# Leave empty or omit to allow all signups (open mode)
# Note: Email matching is case-insensitive
APPROVED_EMAILS=""
